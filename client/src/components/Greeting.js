import React from 'react'

// greeting component displays when user is not looged in
// that way we introduce them to the site, and give them
// the option to either log in or register

const Greeting = () => <p>hi there</p>

export default Greeting
